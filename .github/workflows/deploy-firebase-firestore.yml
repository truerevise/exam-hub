name: Deploy Firebase Firestore Rules

on:
  push:
    branches:
      - main
    paths:
      - 'firestore.rules'
      - 'firebase.json'
      - '.github/workflows/deploy-firebase-firestore.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy-firestore:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Verify Firebase CLI
        run: npx --yes firebase-tools@15.28.1 --version

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

      - name: Deploy Firebase Firestore Rules
        run: npx --yes firebase-tools@15.28.1 deploy --only firestore:rules --project exam-hub-db0eb
